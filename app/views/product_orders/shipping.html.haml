/ Connect Shopping carts show page to this page


%h4
  Please enter or confirm your shipping and delivery information

%table
  %thead
    %th
      Name
    %th
      Email
    %th  
      Phone      
    %th  
      Shipping Address
    %th
      Delivery Date
  %tbody
    %td  
      %textarea{name: "Name"}
        = current_user.name
    %td
      %textarea{type: "email", name: "Email"}
        = current_user.email
    %td
      %textarea{name: "Phone"}
        = current_user.phone    
    %td
      %textarea{name: "Address", rows: 3}
        = current_user.address
    %td
      %input{name: "Date", type: "date", min: (Date.today+4), placeholder: (Date.today+4)}
    %td
      = button_to "Update Shipping Address", edit_user_registration_path, method: 'patch', remote: true     

    / = form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :patch }, remote: true) do |f|
    /   %td  
    /     = f.text_field :name
    /   %td
    /     = f.email_field :email
    /   %td
    /     = f.telephone_field :phone   
    /   %td
    /     = f.text_area :address
    /   %td
    /     %input{name: "Date", type: "date", min: (Date.today+4), placeholder: (Date.today+4)}
    /   %td
    /     = f.submit "Confirm Shipping Address"


= button_to "Checkout", new_product_order_path, method: 'get'

%p 
  Please note that, currently, delivery is only available within Manhattan. All orders must be placed at least four (4) days in advance. So, if you orderd today, the earliest delivery date you could choose will be
  - date = Date.today+4 
  = "#{date.strftime("%b %d, %Y")}"