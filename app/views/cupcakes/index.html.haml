#scoreboard
  #start_game
    %input#start{:type => "button", :value => "start"}/
    #life_holder
      = image_tag('cupcake1.jpg', id: "life1")
      = image_tag('cupcake2.jpg', id: "life2")
      = image_tag('cupcake3.jpg', id: "life3")      

  #cupcake_image
    #topping_model Topping!
    #frosting_model Frosting!
    #ice_cream_model Ice Cream!
    #cookie_model Cookie!

  #cupcake_in_progress
    #topping
    #frosting
    #ice_cream
    #cookie
  .score
    %h5#shapes
      Click On These Falling Ingredients!

      %h5.cookie_text
        = image_tag('cherry.jpg', id: "icon")
        &nbsp; Cookie!
      %h5.ic_text
        = image_tag('cherry.jpg', id: "icon")
        &nbsp; Ice Cream 
      %h5.frosting_text 
        = image_tag('cherry.jpg', id: "icon")
        &nbsp; Frosting
      %h5.topping_text 
        = image_tag('cherry.jpg', id: "icon") 
        &nbsp; Topping 

.timer
.square#board
  #instructions
    %h4
      The goal is to build a cupcake by clicking on the falling images before they disappear. Click on 
      %span{:style => "font-weight:bold;"} ONLY ONE 
      of each type of ingredient: 
      = succeed ", " do
        %span{:style => "color:#ec7124;font-weight:bold;"} cookie
      = succeed ", " do
        %span{:style => "color:#fbe400;font-weight:bold;"} ice cream
      %span{:style => "color:#00a384;font-weight:bold;"} frosting
      and
      = succeed ". " do
        %span{:style => "color:#F17D92;font-weight:bold"} topping  
      Click on two of the same ingredient, and you lose a cupcake (which is the same as losing a life, amirite?). Lose three (3) lives or run out of time and the game is lost. Have fun!

%script
  $('#start').on("click", start_game);
  $('#start').on("click", clock_tick);
  $('#start').on("click", set_timer);

