/*
function token_generated(e, token){
  e.preventDefault();
  //$(this).parent().append("<input type='hidden' name='token' value=" +  window._rails_token + ">").submit();
}

$(document).ready(function() {
  $('body').on('token', '.stripe-button', token_generated);
});
*/
